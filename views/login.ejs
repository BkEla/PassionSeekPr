<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
  <style>
    /* Custom CSS to style the radio buttons as rectangular buttons */
    input[type=radio].styled-radio-button {
      position: absolute;
      opacity: 0;
      z-index: -1;
    }

    input[type=radio].styled-radio-button + label {
      font-size: 1rem;
      color: #fff;
      background-color: #007bff;
      display: inline-block;
      margin-right: 10px;
      padding: 6px 12px;
      cursor: pointer;
      border-radius: 4px;
    }

    input[type=radio].styled-radio-button:checked + label {
      background-color: #0062cc;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <h1>Login</h1>
    <% if (messages.error) {%>
      <%= messages.error %>
    <%} %>
    <form action="/login" method="POST">
      <div class="form-group">
        <label for="email">Email address</label>
        <input type="email" class="form-control" id="email" name="email" required>
      </div>
      <div class="form-group">
        <label for="password">Password</label>
        <input type="password" class="form-control" id="password" name="password" required>
      </div>

      <button type="submit" class="btn btn-primary">Login</button>
    </form>
    <a href="/register" class="btn btn-secondary" style="margin-top:5%" >Register</a>
  </div>

  <!--script>
    // Get the user-type-group div
    const userTypeGroup = document.getElementById('user-type-group');

    // Add a change event listener to the radio buttons
    document.querySelectorAll('input[type=radio][name=user-type]').forEach((radio) => {
      radio.addEventListener('change', () => {
        // Change the label text based on the selected user type
        if (radio.value === 'employee') {
          document.querySelector('label[for=employee]').textContent = 'Employer';
          document.querySelector('label[for=student]').textContent = 'Student';
        } else if (radio.value === 'student') {
          document.querySelector('label[for=employee]').textContent = 'Employer';
          document.querySelector('label[for=student]').textContent = 'Student';
        }
      });
    });
  </script-->
</body>
</html>
